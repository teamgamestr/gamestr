import { AnyToken, TemplateInfo, TokenAdapter, TokenizerState } from "../types";
export declare function tokenize(source: string | undefined, tokenAdapter: TokenAdapter, templateInfos?: TemplateInfo[]): {
    state: TokenizerState;
    tokens: AnyToken[];
};
