/**
 * An amount in satoshis
 */
export type Amount = {
    satoshi: number;
} | {
    satoshi: Promise<number>;
};
export declare const SATS: (amount: number) => Amount;
export declare function resolveAmount(amount: Amount): Promise<{
    satoshi: number;
    millisat: number;
}>;
