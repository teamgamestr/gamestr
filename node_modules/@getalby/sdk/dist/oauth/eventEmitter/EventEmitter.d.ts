import { EventListener, EventName, Token } from "../types";
export declare class EventEmitter {
    private events;
    on(event: EventName, listener: EventListener): void;
    off(event: EventName, listener: EventListener): void;
    emit(event: EventName, payload: Token | Error): void;
}
